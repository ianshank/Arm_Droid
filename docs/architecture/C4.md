# armdroid — C4 Architecture Model

<!-- markdownlint-disable MD012 -->

## Overview

armdroid is a Python robot arm manipulation platform built around a layered v0.2 architecture: a
stable public API, a pure domain layer, explicit orchestration, registry-backed subsystem seams,
and the runtime perception/planning/control/hardware pipeline used for training and rollout. The
system trains in MuJoCo simulation (SAC+HER) and transfers to a real MakerWorld 6-DoF arm over
ESP32 JSON-over-UART. All component boundaries are expressed as `@runtime_checkable` Protocol
interfaces; `armdroid.orchestration.factory` is the composition root that wires named
implementations together.

---

## Level 1 — System Context

```mermaid
graph TD
    operator["Robot Arm Operator\n[Person]\nRuns training, rollout,\nand sim subcommands"]

    armdroid["armdroid\n[Software System]\nMakerWorld ESP32 manipulation platform:\nSAC+HER RL training, PDDL planning,\nYOLO perception, MuJoCo simulation"]

    arm["MakerWorld ESP32 arm\n[External Hardware]\n6-DoF robot arm, ESP32 JSON-over-UART\n(USB serial, 115 200 baud)"]

    camera["RealSense D435i\n[Optional External Hardware]\nRGB-D depth camera for\nlive object detection"]

    claude["Claude API\n[Optional External Service]\nLLM-backed adaptive replanner\n(claude-sonnet-4-6, Anthropic)"]

    mujoco["MuJoCo\n[External Library]\nPhysics simulation engine\nfor RL training environments"]

    isaac["NVIDIA Isaac Sim 5.1 / Isaac Lab 2.3\n[Optional External Runtime — PR-B + F1]\nSingle-env protocol path AND\nvectorised path (num_envs > 1)\nfor parallel RSL-RL PPO training.\nBehind [isaac] extra."]

    operator -->|"CLI: train / rollout / sim"| armdroid
    armdroid -->|"Joint commands / state reads\nasync serial"| arm
    armdroid -->|"Depth + RGB frames\n(pyrealsense2)"| camera
    armdroid -->|"Replanning prompts\n(HTTPS REST)"| claude
    armdroid -->|"Environment step / reset\n(in-process)"| mujoco
    armdroid -->|"Single-env (num_envs=1) OR\nvec env (num_envs > 1)\nUSD articulation + RSL-RL PPO"| isaac
```

---

## Level 2 — Container Diagram

```mermaid
graph TD
    subgraph armdroid ["armdroid — Runtime Containers"]
        cli["CLI\n[armdroid.main]\nArgparse entry point.\nDispatches train / rollout / sim.\nLoads YAML overlays + env vars."]

        cfg["Config System\n[ArmSettings + YAML loader]\nPydantic v2 BaseSettings.\nARMDROID_ env prefix.\nYAML overlay merging.\nSingle source of truth."]

        orch["ArmOrchestrator\n[orchestration/orchestrator.py]\nLifecycle coordinator.\ntrain() — synchronous SB3 loop.\nrollout() — async plan+execute.\nshutdown() — driver teardown.\nHolds optional task_cfg for resolving\nPDDL plan-step args to Cartesian XYZ\nvia _resolve_target_position (TD-5)."]

        perception["Perception Pipeline\n[ArmPerception facade]\nDepth processing (D435i).\nYOLO11 object detection (CUDA).\nOpenCV PnP 6-DoF target pose estimation\n(2D keypoints mapped to 3D geometry).\nVision -> PDDL symbolic state."]

        planner["Symbolic Planner\n[SymbolicPlanner + Replanner]\nPDDL domain + problem generation.\nPyperplan BFS/A* search.\nOptional LLM adaptive replanner\n(Claude API backend)."]

        controller["RL Controller\n[ArmController]\nSAC+HER goal-conditioned policy\n(Stable-Baselines3).\nActionPrimitives: grasp/place/transit.\nTrajectoryGenerator: cubic interpolation."]

        env["Gymnasium Environment\n[TowerOfHanoiEnv / LaundrySortingEnv /\nSoArmReachIsaacEnv / SoArmReachIsaacVecEnv]\nMuJoCo GoalEnv (single-env path).\nIsaac Lab ManagerBasedRLEnv via\n_TensorAdapter (num_envs=1) OR direct\ntorch-tensor vec env (num_envs > 1, F1).\nDomain randomization (friction/mass/PD noise).\nReward shaping. Curriculum difficulty.\nFactory dispatch on cfg.arm_sim_isaac.num_envs."]

        driver["Arm Driver\n[registry -> MockArmDriver / Esp32JsonDriver / IsaacSimDriver]\nResolved by orchestration._driver_kind.resolve_driver_kind(cfg)\nfrom arm_driver_kind | legacy mock_hardware bool.\nEsp32JsonDriver is transport-pluggable (Phase 1.0):\nSerialTransport (USB-UART) | TcpTransport (Wi-Fi)\n| BleTransport (BLE GATT, [ble] extra) selected via\nArmTransportConfig.protocol. HMAC-SHA256 auth on the\nwireless variants. IsaacSimDriver drives a USD\narticulation under Isaac Lab 2.3 (opt-in [isaac] extra)."]
    end

    user["Operator"]
    hw["MakerWorld ESP32 arm"]
    cam["RealSense D435i"]
    llm["Claude API"]
    sim["MuJoCo"]

    user -->|"CLI args"| cli
    cli -->|"load_settings()"| cfg
    cli -->|"build_arm_orchestrator(cfg)"| orch
    orch -->|"detect_objects()\nget_symbolic_state()"| perception
    orch -->|"plan()\nreplan()"| planner
    orch -->|"build_for_env()\ntrain_policy()\nexecute_primitive()"| controller
    orch -->|"reset()\nstep()"| env
    orch -->|"start() / stop()"| driver
    controller -->|"grasp / place / transit\nvia driver"| driver
    planner -->|"LLM prompt\n(optional)"| llm
    perception -->|"RGB-D frames"| cam
    driver -->|"JSON-over-UART commands"| hw
    env -->|"physics step"| sim
```

---

## Level 3 — Component Diagram: RL Controller

```mermaid
graph TD
    subgraph controller ["RL Controller — ArmController (control/)"]
        ac["ArmController\n[controller.py]\nImplements ArmControllerProtocol.\nbuild_for_env(env) — dispatches build()\nvs build_vec() on env protocol type (F1).\ntrain_policy() — mirrors with train_vec().\nexecute_primitive() — ActionPrimitives.\nexecute_action() — continuous delta actions.\nRaises TypeError when env satisfies neither\nprotocol (no silent degradation)."]

        sac["SACAgent\n[sac_agent.py]\nSingle-env only (ArmRLAgentProtocol).\nWraps SB3 SAC + HerReplayBuffer.\nbuild(env) — constructs MultiInputPolicy.\ntrain(timesteps) — model.learn().\npredict(obs) — torch.no_grad() inference."]

        rsl["RslRlPpoAgent\n[rsl_rl_agent.py]\nImplements BOTH ArmRLAgentProtocol\nAND VecArmRLAgentProtocol (F1).\nbuild() — single-env path via legacy\nenv._isaac_env reach-through.\nbuild_vec(env) — clean as_runner_env()\naccessor; no reach-through.\ntrain() / train_vec() share _iterations_for.\nDevice from ppo_cfg.device (config-driven)."]

        prim["ActionPrimitives\n[primitives.py]\nHigh-level manipulation library.\ngrasp / place / transit / home."]

        traj["TrajectoryGenerator\n[trajectory.py]\nCubic / linear interpolation.\nVelocity clamping. Joint bounds clip."]

        driver_ref["ArmDriverProtocol\n[via ActionPrimitives]\nAsync hardware boundary."]
    end

    ac -->|"build() / train()\n(single-env)"| sac
    ac -->|"build() OR build_vec()\ntrain() OR train_vec()\n(dispatch on env protocol type)"| rsl
    ac -->|"grasp() / place() / transit() / home()"| prim
    prim -->|"interpolated waypoints"| traj
    prim -->|"send_joint_command()\nclose_gripper() / open_gripper()\nhome()"| driver_ref

    env_ref["env: ArmEnvironmentProtocol |\nVecArmEnvironmentProtocol (F1)"]
    orch_ref["ArmOrchestrator"]

    orch_ref -->|"build_for_env(env)"| ac
    env_ref -->|"isinstance dispatch in\nbuild_for_env"| ac
```

---

## Level 3 — Component Diagram: Source Package Layout (v0.2+)

```mermaid
graph TD
    subgraph domain ["armdroid.domain (no internal imports)"]
        d_state["domain.state\nArmState · PlanStep\nSymbolicState · DetectedObject\n+ ArmAction (Phase A)\n+ SceneInsight (Phase A)\n+ Verdict (Phase A)\n+ InteractionEvent (Phase A)"]
        d_err["domain.errors\nArmError hierarchy\n(ArmDriverError · ArmCommandRejected\nArmPlanningError · ArmPerceptionError\nArmConfigurationError)"]
        d_proto["domain.protocols\n@runtime_checkable Protocols\nArmDriverProtocol\nArmControllerProtocol\nArmPlannerProtocol\nArmEnvironmentProtocol\nArmPerceptionProtocol\nArmRLAgentProtocol\n+ VecArmEnvironmentProtocol (F1)\n+ VecArmRLAgentProtocol (F1)\n+ VisionLanguageAgentProtocol (Phase A,\n  disjoint methods build_vla / act /\n  is_vla_built / is_vla_loaded per ADR-0007)\n+ HighLevelPlannerProtocol (Phase A)\n+ SafetyGuardProtocol (Phase A)\n+ InteractionSessionProtocol (Phase A)"]
        d_units["domain.units\nRadians · Metres · Seconds"]
    end

    subgraph config ["armdroid.config"]
        c_schema["config.schema.*\nMulti-module package\nArmSettings · ArmConfig\nTransportConfig · MotionConfig\nControlConfig · PerceptionConfig\n(widened: detector_kind, frame_buffer,\nopen_vocab_max_queries, gemini_* fields)\nPlanningConfig · SimConfig\nArmSimIsaacConfig · RslRlPpoConfig\nLLMReplannerConfig (widened: gemini backend,\napi_endpoint, safety_tier, transport)\n+ ArmVlaConfig (Phase A)\n+ ArmSafetyConfig (Phase A)\n+ ArmInteractionConfig (Phase A)"]
        c_loader["config.loader\nload_settings(path)\nYAML overlay + env merge"]
    end

    subgraph adapters ["Adapters (import domain, not each other)"]
        hw["hardware.*\nMockArmDriver\nEsp32JsonDriver (hardware/esp32/)\nIsaacSimDriver (hardware/isaac_sim/, PR-B)\ndriver_registry"]
        plan["planning.*\nSymbolicPlanner · Replanner\nPDDLDomain · LLM backends\nplanner_registry"]
        perc["perception.*\nArmPerception facade\nDepthProcessor · ObjectDetector\nPoseEstimator · StateExtractor\nperception_registry"]
        ctrl["control.*\nArmController (dispatches single-env vs vec)\nSACAgent (single-env only)\nRslRlPpoAgent (both protocols, F1)\nActionPrimitives · TrajectoryGenerator\nrl_agent_registry: type[ArmRLAgentProtocol]"]
        envs["environments.*\nTowerOfHanoiEnv · LaundrySortingEnv\nSoArmReachIsaacEnv (environments/isaac/, PR-B)\nSoArmReachIsaacVecEnv (F1, num_envs >= 1,\n_app_state Kit coordination)\nDomainRandomizer · CurriculumManager\nenvironment_registry (single-env)\nregistry_vec (F1, sibling for vec env)"]
    end

    subgraph orch ["armdroid.orchestration"]
        o_fac["orchestration.factory\nbuild_arm_driver()\nbuild_arm_controller() — registry-dispatched\nbuild_arm_environment() — dispatches\nsingle-env vs vec on cfg.arm_sim_isaac.num_envs\n+ _VEC_TASK_REGISTRY_NAMES mapping (F1)\nbuild_arm_orchestrator()"]
        o_kind["orchestration._driver_kind\nresolve_driver_kind(cfg)\narm_driver_kind > legacy mock_hardware bool\n(once-per-process DeprecationWarning)"]
        o_orch["orchestration.orchestrator\nArmOrchestrator\ntrain() · rollout() · shutdown()"]
    end

    subgraph cli ["CLI / stable public API"]
        main["armdroid.main\ncli_entry()\nargparse dispatch"]
        api["armdroid.api + armdroid\nStable SemVer public surface\nRe-exports settings, state,\nprotocols, errors, factory helpers,\nand ArmOrchestrator"]
    end

    subgraph compat ["armdroid.compat (deprecation bridge)"]
        shims["Shim modules\nprotocols.py · factory.py\norchestrator.py\nhardware/esp32_json_driver.py"]
        adapter["LegacyArmDriverAdapter\nmaps v0.1 method names\nto v0.2 API"]
    end

    domain --> config
    domain --> adapters
    config --> adapters
    adapters --> orch
    domain --> orch
    orch --> api
    domain --> api
    api --> main
    domain --> compat
    adapters --> compat
```

---

## Level 3 — Component Diagram: Vec Env Path (F1)

```mermaid
graph TD
    subgraph orch_layer ["Orchestration"]
        of["build_arm_environment(cfg)\n[factory.py]\nDispatches on _should_use_vec(cfg)\n+ _VEC_CAPABLE_ALGORITHMS\n+ _VEC_TASK_REGISTRY_NAMES"]
        bc["ArmController.build_for_env(env)\n[controller.py]\nisinstance(env, VecArmEnvironmentProtocol)\n-> agent.build_vec(env)\nelse -> agent.build(env)"]
    end

    subgraph registry_layer ["Registry Layer"]
        reg_single["registry.py\n_ENVIRONMENTS:\nRegistry[EnvironmentFactory]\nentry-point group: armdroid.environments"]
        reg_vec["registry_vec.py (F1)\n_VEC_ENVIRONMENTS:\nRegistry[VecEnvironmentFactory]\nentry-point group: armdroid.vec_environments\nLazy isaaclab probe; runtime\nisinstance check on factory result."]
    end

    subgraph env_layer ["Environment Implementations"]
        single_env["SoArmReachIsaacEnv\n[environments/isaac/reach.py]\nArmEnvironmentProtocol\nnum_envs == 1 only\n_TensorAdapter bridges torch -> numpy\nLegacy env._isaac_env reach-through\n(preserved for backwards-compat)"]
        vec_env["SoArmReachIsaacVecEnv (F1)\n[environments/isaac/reach_vec.py]\nVecArmEnvironmentProtocol\nnum_envs >= 1 (degenerate=1 allowed)\nNO _TensorAdapter (torch-native)\nas_runner_env() - clean accessor\n_app_state coordination on first boot"]
    end

    subgraph runtime_layer ["Isaac Runtime"]
        kit["_app_state singleton\n[hardware/isaac_sim/_app_state.py]\nis_app_launched() probe\nmark_launched()\nreset_for_tests()\nShared between driver + vec env."]
        mbr["ManagerBasedRLEnv\n[isaaclab.envs]\nVectorised torch-tensor returns.\nOwned directly by vec env;\nas_runner_env() returns this for\nRSL-RL OnPolicyRunner."]
    end

    subgraph agent_layer ["RSL-RL Agent"]
        rsl_agent["RslRlPpoAgent\n[control/rsl_rl_agent.py]\nbuild() -> env._isaac_env (single-env)\nbuild_vec(env) -> env.as_runner_env() (F1)\nShared _iterations_for(total_timesteps).\nDevice from ppo_cfg.device (config-driven)."]
    end

    of -->|"num_envs == 1"| reg_single
    of -->|"num_envs > 1"| reg_vec
    reg_single -->|"factory()"| single_env
    reg_vec -->|"factory()"| vec_env
    single_env -->|"reach-through"| mbr
    vec_env -->|"owns directly"| mbr
    vec_env -->|"_ensure_built() coordinates"| kit
    bc -->|"build_vec(env)"| rsl_agent
    rsl_agent -->|"as_runner_env()"| vec_env
    bc -->|"build(env)"| rsl_agent
```

See [ADR-0006](ADR/ADR-0006-vec-env-protocol.md) for the eight
architectural decisions behind this layout.

---

## Level 3 — Phase A scaffolding: Gemini Robotics ER 1.6 (`feature/gemini-er`)

Phase A of the Gemini ER 1.6 integration plan lands every wiring slot
that Phases B-F (perception, planning, VLA, Live API, safety) need,
without changing any default behaviour. The diagram below shows the
new protocol slots, config sub-models, and telemetry SPAN families
declared in Phase A; concrete backends light up in subsequent phases.

```mermaid
graph LR
    subgraph phase_a ["Phase A scaffolding (this PR)"]
        pa_protocols["domain.protocols (new)\nVisionLanguageAgentProtocol\n(disjoint methods, ADR-0007)\nHighLevelPlannerProtocol\nSafetyGuardProtocol\nInteractionSessionProtocol"]
        pa_state["domain.state (widened)\nArmAction (frozen, slotted)\nSceneInsight · Verdict\nInteractionEvent\nDetectedObject + 5 kw-only fields\n(affordances, is_fragile, is_fixed,\nsemantic_tags, text_query)"]
        pa_cfg["config.schema (new sub-models)\nArmVlaConfig (enabled=False default)\nArmSafetyConfig (asimov_rule_set=off)\nArmInteractionConfig (enabled=False)\nLLMReplannerConfig.backend +'gemini'\nArmPerceptionConfig.detector_kind"]
        pa_tel["telemetry SPAN families (declared)\nSPAN_PERCEPTION_* (4)\nSPAN_LLM_REPLAN_* (3)\nSPAN_VLA_* (3)\nSPAN_INTERACTION_* (4)\nSPAN_SAFETY_* (3)"]
        pa_extras["[gemini] · [gemini-live]\n· [gemini-vla] extras\n+ wheel asset slot\nassets/safety -> armdroid/safety/data"]
        pa_helper["tests/helpers/fake_llm_sdk\nfake_anthropic_sdk\nfake_gemini_sdk\nfake_llm_sdk (generic)"]
    end

    subgraph phase_b ["Phase B (perception, future PR)"]
        pb["GeminiObjectDetector\nFrameBuffer · SceneReasoner\ndetector_registry"]
    end

    subgraph phase_c ["Phase C (planning, future PR)"]
        pc["GeminiReplanner\nllm_replanners registry\nReplanner factory wiring"]
    end

    subgraph phase_d ["Phase D (VLA, future PR)"]
        pd["GeminiVlaAgent\nvision_agents registry\nArmController VLA dispatch"]
    end

    subgraph phase_e ["Phase E (Live API, future PR)"]
        pe["GeminiLiveSession\ninteraction_sessions registry"]
    end

    subgraph phase_f ["Phase F (Safety, future PR)"]
        pf["IsoKinematicGuard\nAsimovGuard · FragileObjectGuard\nswiss_cheese chain\nNEISS corpus in wheel"]
    end

    pa_protocols --> pb
    pa_protocols --> pc
    pa_protocols --> pd
    pa_protocols --> pe
    pa_protocols --> pf
    pa_state --> pb
    pa_state --> pd
    pa_state --> pf
    pa_cfg --> pb
    pa_cfg --> pc
    pa_cfg --> pd
    pa_cfg --> pe
    pa_cfg --> pf
    pa_tel --> pb
    pa_tel --> pc
    pa_tel --> pd
    pa_tel --> pe
    pa_tel --> pf
    pa_extras --> pb
    pa_extras --> pc
    pa_extras --> pd
    pa_extras --> pe
    pa_extras --> pf
    pa_helper --> pb
    pa_helper --> pc
    pa_helper --> pd
```

See [ADR-0007](ADR/ADR-0007-vision-language-agent-protocol.md),
[ADR-0008](ADR/ADR-0008-open-vocabulary-detector.md), and
[ADR-0009](ADR/ADR-0009-safety-middleware.md) for the three Phase A
architectural decisions, and the plan at
`/root/.claude/plans/as-per-the-research-radiant-nova.md` for the
phased implementation schedule.

Backwards-compatibility guarantees (pinned by regression tests):

* `DetectedObject` positional six-arg ctor unchanged. `__slots__`
  tuple pinned by
  [`tests/regression/test_detected_object_compat.py`](../../tests/regression/test_detected_object_compat.py).
* Phase A public-API surface (new value objects + protocols) reachable
  through `armdroid`, `armdroid.api`, and `armdroid.domain` with
  identity-checked re-exports pinned by
  [`tests/regression/test_public_api_phase_a.py`](../../tests/regression/test_public_api_phase_a.py).
* New YAML overlays (`config/examples/gemini.example.yaml`) load
  cleanly with all flags disabled; unknown keys silently dropped
  (pydantic-settings v2 default).
* `pip install -e ".[dev]"` does not pull `google-genai`; the
  `[gemini]`, `[gemini-live]`, `[gemini-vla]` extras are opt-in.

---

## Key Design Decisions

### Layered architecture (v0.2+)

The codebase is organised into five explicit layers, each allowed to import only from layers below it:

```text
┌─────────────────────────────────────────────┐
│  CLI / public API  (armdroid.main, __main__) │
├─────────────────────────────────────────────┤
│  Orchestration     (armdroid.orchestration)  │
│    factory.py  ·  orchestrator.py            │
├────────────────┬────────────────────────────┤
│  Adapters      │  Config                    │
│  hardware/     │  armdroid.config           │
│  planning/     │  schema.*                  │
│  perception/   │                            │
│  control/      │                            │
│  environments/ │                            │
├────────────────┴────────────────────────────┤
│  Domain  (armdroid.domain)                  │
│  state · errors · protocols · units         │
└─────────────────────────────────────────────┘
```

The `armdroid.domain` package has **no internal armdroid imports** — it is a pure-Python island
that resolves in under 10 ms on a cold interpreter. See [ADR-0002](ADR/ADR-0002-domain-layer.md).

### Protocol-based dependency injection

Every subsystem boundary is a `@runtime_checkable Protocol` defined in `armdroid.domain.protocols`.
The `ArmOrchestrator` holds references typed as `ArmDriverProtocol`, `ArmPerceptionProtocol`, etc.
Concrete types (`MockArmDriver`, `Esp32JsonDriver`, `SACAgent`, …) are imported only inside
`armdroid.orchestration.factory`. This means tests can inject plain Python objects that satisfy the
structural protocol without inheriting from any base class, and swapping implementations requires no
changes outside the factory module.

### Registry-based extension points (v0.2+)

Each subsystem exposes a `Registry[T]` singleton populated at import time. Out-of-tree packages
declare implementations via `[project.entry-points."armdroid.drivers"]` (etc.) in their
`pyproject.toml` — no fork required. The current composition root resolves drivers and
environments through these registries, and the same seam is available for the other subsystems as
their composition points are expanded. See [ADR-0003](ADR/ADR-0003-registry-pattern.md).

### Shim deprecation strategy (v0.2+)

Moved modules (`armdroid.protocols`, `armdroid.factory`, `armdroid.orchestrator`,
`armdroid.hardware.esp32_json_driver`) are preserved as shims that emit `DeprecationWarning` on
import and re-export all previously public names. Shims will be deleted at v0.4.0. For migration
instructions see [docs/migration/v0.1-to-v0.2.md](../migration/v0.1-to-v0.2.md). See also
[ADR-0004](ADR/ADR-0004-shim-deprecation.md).

### Async / sync boundary

Stable-Baselines3 `SAC.learn()` is a synchronous, blocking call — it owns the Python thread for
the duration of training. All hardware I/O (`ArmDriverProtocol`, `ArmPerceptionProtocol`) is
`async`. The boundary is explicit: `ArmOrchestrator.train()` is synchronous (called from
`asyncio.run()` wrapper in `main.py`), while `ArmOrchestrator.rollout()` and `shutdown()` are
`async`. ActionPrimitives methods are `async` and are awaited by the synchronous controller only
during rollout, never during the SB3 training loop.

### Factory pattern — single wiring point

`armdroid.orchestration.factory` contains one `build_*()` function per protocol. The top-level
`build_arm_orchestrator(cfg)` constructs the driver once, resolves the configured driver and
environment through typed registries, and passes the shared driver to both
`build_arm_controller()` and the orchestrator directly. This keeps exactly one serial connection
open to the MakerWorld ESP32 arm. The orchestrator itself does zero construction — it only stores
and coordinates the pre-built components.

### ARMDROID_ environment variable prefix

`ArmSettings` uses `pydantic-settings` with `env_prefix="ARMDROID_"` and
`env_nested_delimiter="__"`. Any config field can be overridden at runtime without touching YAML:
`ARMDROID_ARM__TRANSPORT__SERIAL_PORT=COM5`, `ARMDROID_MOCK_HARDWARE=true`, etc. This matches the
MouseDroidAGI parent project's `MOUSEDROID_` convention and keeps CI/CD environment injection
clean.

See the ADRs in this directory for the detailed rationale behind the v0.2 layering,
registry, and compatibility decisions.

