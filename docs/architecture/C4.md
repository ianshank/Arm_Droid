# armdroid — C4 Architecture Model

## Overview

armdroid is a Python robot arm manipulation platform built around a four-layer hierarchical
architecture: vision-based perception, PDDL symbolic planning, goal-conditioned RL control, and
physical hardware execution. The system trains in MuJoCo simulation (SAC+HER) and transfers to a
real SO-ARM100 6-DoF arm over USB serial. All component boundaries are expressed as
`@runtime_checkable` Protocol interfaces; `factory.py` is the single dependency-injection wiring
point.

---

## Level 1 — System Context

```mermaid
graph TD
    operator["Robot Arm Operator\n[Person]\nRuns training, rollout,\nand sim subcommands"]

    armdroid["armdroid\n[Software System]\nSO-ARM100 manipulation platform:\nSAC+HER RL training, PDDL planning,\nYOLO perception, MuJoCo simulation"]

    arm["SO-ARM100\n[External Hardware]\n6-DoF robot arm over\nUSB serial (COM3, 115 200 baud)"]

    camera["RealSense D435i\n[Optional External Hardware]\nRGB-D depth camera for\nlive object detection"]

    claude["Claude API\n[Optional External Service]\nLLM-backed adaptive replanner\n(claude-sonnet-4-6, Anthropic)"]

    mujoco["MuJoCo\n[External Library]\nPhysics simulation engine\nfor RL training environments"]

    operator -->|"CLI: train / rollout / sim"| armdroid
    armdroid -->|"Joint commands / state reads\nasync serial"| arm
    armdroid -->|"Depth + RGB frames\n(pyrealsense2)"| camera
    armdroid -->|"Replanning prompts\n(HTTPS REST)"| claude
    armdroid -->|"Environment step / reset\n(in-process)"| mujoco
```

---

## Level 2 — Container Diagram

```mermaid
graph TD
    subgraph armdroid ["armdroid — Runtime Containers"]
        cli["CLI\n[python -m armdroid]\nArgparse entry point.\nDispatches train / rollout / sim.\nLoads YAML overlays + env vars."]

        cfg["Config System\n[ArmSettings + YAML loader]\nPydantic v2 BaseSettings.\nARMDROID_ env prefix.\nYAML overlay merging.\nSingle source of truth."]

        orch["ArmOrchestrator\n[orchestrator.py]\nLifecycle coordinator.\ntrain() — synchronous SB3 loop.\nrollout() — async plan+execute.\nshutdown() — driver teardown."]

        perception["Perception Pipeline\n[ArmPerception facade]\nDepth processing (D435i).\nYOLO11 object detection (CUDA).\nPnP 6-DoF pose estimation.\nVision -> PDDL symbolic state."]

        planner["Symbolic Planner\n[SymbolicPlanner + Replanner]\nPDDL domain + problem generation.\nPyperplan BFS/A* search.\nOptional LLM adaptive replanner\n(Claude API backend)."]

        controller["RL Controller\n[ArmController]\nSAC+HER goal-conditioned policy\n(Stable-Baselines3).\nActionPrimitives: grasp/place/transit.\nTrajectoryGenerator: cubic interpolation."]

        env["Gymnasium Environment\n[TowerOfHanoiEnv / LaundrySortingEnv]\nMuJoCo GoalEnv.\nDomain randomization.\nConfigurable reward shaping.\nCurriculum difficulty manager."]

        driver["Arm Driver\n[SoArm100Driver / MockArmDriver]\nAsync serial driver (pyserial).\nJoint state reads + commands.\nGripper open/close.\nEmergency stop + home."]
    end

    user["Operator"]
    hw["SO-ARM100"]
    cam["RealSense D435i"]
    llm["Claude API"]
    sim["MuJoCo"]

    user -->|"CLI args"| cli
    cli -->|"load_settings()"| cfg
    cli -->|"build_arm_orchestrator(cfg)"| orch
    orch -->|"detect_objects()\nget_symbolic_state()"| perception
    orch -->|"plan()\nreplan()"| planner
    orch -->|"build_for_env()\ntrain_policy()\nexecute_primitive()"| controller
    orch -->|"reset()\nstep()"| env
    orch -->|"start() / stop()"| driver
    controller -->|"grasp / place / transit\nvia driver"| driver
    planner -->|"LLM prompt\n(optional)"| llm
    perception -->|"RGB-D frames"| cam
    driver -->|"serial commands"| hw
    env -->|"physics step"| sim
```

---

## Level 3 — Component Diagram: RL Controller

```mermaid
graph TD
    subgraph controller ["RL Controller — ArmController (control/)"]
        ac["ArmController\n[controller.py]\nImplements ArmControllerProtocol.\nbuild_for_env() — lazy SACAgent wiring.\ntrain_policy() — delegates to SACAgent.\nexecute_primitive() — dispatches to ActionPrimitives.\nexecute_action() — continuous delta actions."]

        sac["SACAgent\n[sac_agent.py]\nWraps SB3 SAC + HerReplayBuffer.\nbuild(env) — constructs MultiInputPolicy.\ntrain(timesteps) — calls model.learn().\npredict(obs) — torch.no_grad() inference.\nsave() / load() — checkpoint management."]

        prim["ActionPrimitives\n[primitives.py]\nHigh-level manipulation library.\ngrasp(pose) — approach + close_gripper().\nplace(pose) — move + open_gripper().\ntransit(pose) — send_joint_command().\nhome() — driver.home()."]

        traj["TrajectoryGenerator\n[trajectory.py]\nCubic / linear interpolation.\ninterpolate(start, end, n_steps).\nsmooth(waypoints, dt) — velocity clamping.\nenforce_limits() — joint bounds clip."]

        driver_ref["ArmDriverProtocol\n[via ActionPrimitives]\nAsync hardware boundary.\nAll motor commands flow through here."]
    end

    ac -->|"build() / train() / predict() / save()"| sac
    ac -->|"grasp() / place() / transit() / home()"| prim
    prim -->|"interpolated waypoints"| traj
    prim -->|"send_joint_command()\nclose_gripper() / open_gripper()\nhome()"| driver_ref

    env_ref["ArmEnvironmentProtocol\n[from Orchestrator]"]
    orch_ref["ArmOrchestrator"]

    orch_ref -->|"build_for_env(env)"| ac
    env_ref -->|"passed to SACAgent.build()"| sac
```

---

## Key Design Decisions

### Protocol-based dependency injection

Every subsystem boundary is a `@runtime_checkable Protocol` defined in `protocols.py`. The
`ArmOrchestrator` holds references typed as `ArmDriverProtocol`, `ArmPerceptionProtocol`, etc.
Concrete types (`MockArmDriver`, `SoArm100Driver`, `SACAgent`, …) are imported only inside
`factory.py`. This means tests can inject plain Python objects that satisfy the structural protocol
without inheriting from any base class, and swapping implementations (e.g. mock for real driver)
requires no changes outside `factory.py`.

### Async / sync boundary

Stable-Baselines3 `SAC.learn()` is a synchronous, blocking call — it owns the Python thread for
the duration of training. All hardware I/O (`ArmDriverProtocol`, `ArmPerceptionProtocol`) is
`async`. The boundary is explicit: `ArmOrchestrator.train()` is synchronous (called from
`asyncio.run()` wrapper in `main.py`), while `ArmOrchestrator.rollout()` and `shutdown()` are
`async`. ActionPrimitives methods are `async` and are awaited by the synchronous controller only
during rollout, never during the SB3 training loop.

### Factory pattern — single wiring point

`factory.py` contains one `build_*()` function per protocol. The top-level
`build_arm_orchestrator(cfg)` constructs the driver once and passes it to both
`build_arm_controller()` and the orchestrator directly, ensuring exactly one serial connection is
opened to the SO-ARM100. The orchestrator itself does zero construction — it only stores and
coordinates the pre-built components.

### ARMDROID_ environment variable prefix

`ArmSettings` uses `pydantic-settings` with `env_prefix="ARMDROID_"` and
`env_nested_delimiter="__"`. Any config field can be overridden at runtime without touching YAML:
`ARMDROID_ARM__SERIAL_PORT=COM5`, `ARMDROID_MOCK_HARDWARE=true`, etc. This matches the
MouseDroidAGI parent project's `MOUSEDROID_` convention and keeps CI/CD environment injection
clean.
